# Django Framework (similar to Express.js in Node.js)
Django==4.2.7

# Django REST Framework (similar to Express + middleware for REST APIs)
djangorestframework==3.14.0

# PostgreSQL adapter (similar to 'pg' package in Node.js)
psycopg2-binary==2.9.9

# Environment variables support (similar to 'dotenv' in Node.js)
python-decouple==3.8

# CORS headers (similar to 'cors' package in Node.js)
django-cors-headers==4.3.1

# Filtering support for querysets (similar to building WHERE clauses manually in SQL)
django-filter==23.5

# DRF GIS serializers for GeoJSON support
djangorestframework-gis==1.0

# For better development experience
django-extensions==3.2.3

# Timezone support
pytz==2023.3
